# Core: the OpenAI SDK (also reaches local OpenAI-compatible servers) +
# python-dotenv to load your .env.
openai>=2.0,<3
python-dotenv>=1.0.0

# For PROVIDER=claude. Harmless to install even if you only use OpenAI.
anthropic>=0.111.0,<1

# Pretty tables/markdown in the hands_on/optimize.py capstone.
rich>=13.0.0
